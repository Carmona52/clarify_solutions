
const config = {
    theme:{
        extend: {
            fontFamily:{
                'principal': ['var(--font-hurme-font)','sans-serif'],
            }
        }
    },
    plugins: {
        "@tailwindcss/postcss": {},
    },
};

export default config;
